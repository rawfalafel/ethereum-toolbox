package main

func Encode(val interface{}) byte[] {
	// Before marshaling, determine the length of the final array
	// Determine type with reflection library
	// For structs and arrays, encode recursively
}
